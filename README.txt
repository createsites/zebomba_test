Общее время выполнения тестового задания: 14 часов.

Вызвало сложности:
- При обработке изображений gulp их ломал, они становились в 2 раза больше размером и не открывались.
Решением стал даунгрейд версии gulp с 5 на 4. Заняло около получаса.
- Оформление таблицы рейтинга заняло времени больше предполагаемого: 1.5 часа.

Предложения по улучшению:
- Кнопки (например "В универ") сверстать с помощью CSS3 (применив linear-gradient для background и border, box-shadow).
В текущем задании так не сделал, т.к. получилось бы не pixel perfect.